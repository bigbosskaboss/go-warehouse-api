CREATE TABLE if not exists items (
                                     id INT PRIMARY KEY UNIQUE,
                                     name VARCHAR(255),
                                     size VARCHAR(255)
);